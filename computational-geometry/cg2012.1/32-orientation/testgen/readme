В общем, тут есть генератор тестов, который просто надо собрать и запустить — он рандомно генерирует 10 тестов тупых и 50 таких, что на них будет вызываться adaptive precision (или multiprecision, в общем, фильтрованный предикат). Он всегда генерирует одно и то же, так как seed я захардкодил. Если хочется его поменять — он в начале кода, после инклудов.
