\begin{problem}{04. ѕринадлежность точки произвольному многоугольнику}{standard
input}{standard output}{2 секунды}{256 мегабайт}

Ќа плоскости заданы точки и некоторый произвольный многоугольник с дырками.
Ќеобходимо определить, принадлежат ли они этому многоугольнику.

\InputFile

ѕерва€ строка входного файла содержит целое число $n$ ---
количество вершин многоугольника. ¬ последующих $n$ строчках содержатс€ пары
вещественных чисел $(x_i, y_i)$ --- координаты $i$-ой вершины многоугольника.
¬ следующей строке содержитс€ целое число $m$ --- количество дырок в
многоугольнике.
¬ последующих $m$ строчках содержитс€ информаци€ о дырках. ¬ $j$-ой строке
содержитс€ число $m_j$ --- количество вершин в дырке, а затем $m_j$ пар
вещественных чисел $(x_{m_j}, y_{m_j})$ --- координаты этих вершин.
—ледующа€ строка содержит целое число $l$ --- количество точек, дл€ которых
требуетс€ ответить, принадлежат ли они многоугольнику.
¬ последующих $l$ строчках содержатс€ пары вещественных чисел $(x_k, y_k)$ ---
координаты точек. ¬ершины многоугольника заданы в пор€дке обхода против часовой
стрелки, вершины дырок - по часовой стрелке.

\OutputFile

¬ выходной файл выведите $l$ строк, содержащих $1$, если $k$-а€ точка внутри
пр€моугольника, $0$ --- на границе, $-1$ --- снаружи.

\Examples

\begin{example}%
\exmp{
4
0 0 
10 0
10 10
0 10
0
1
10 4
}{
0
}%
\exmp{
3
0 0
10 0.5
0 1
1
3
0.1 0.1
0.1 0.9
9 0.5
1
5 0.5
}{
-1
}%
\end{example}
\end{problem}